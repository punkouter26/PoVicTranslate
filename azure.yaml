# Azure Developer CLI (azd) configuration
name: PoVicTranslate
metadata:
  template: azd-init

services:
  web:
    project: ./src/PoVicTranslate.Web
    host: containerapp
    language: dotnet

hooks:
  preprovision:
    shell: pwsh
    run: |
      Write-Host "Using existing resources from PoShared resource group"
