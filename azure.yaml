# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: PoVicTranslate
metadata:
  template: PoVicTranslate@0.0.1
services:
  web:
    project: ./VictorianTranslator.Server
    language: csharp
    host: appservice
infra:
  provider: bicep
