# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json

name: PoVicTranslate
infra:
  provider: bicep
services:
  victorianupdater-server:
    project: VictorianTranslator.Server
    host: appservice
    language: dotnet
